set(files "heap" "merge" "qsort" "qsort3way" "bubble" "shell" "radix" "selection" "insertion" "qsortParallel")
foreach(file ${files})
	add_library(${file} SHARED "${file}.cpp")
endforeach()
set(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/lib)
